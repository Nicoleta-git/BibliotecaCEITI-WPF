<UserControl x:Class="BibliotecaCEITI.Students"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa5="http://schemas.fontawesome.com/icons/"
             xmlns:local="clr-namespace:BibliotecaCEITI"
             mc:Ignorable="d" 
             Background="{DynamicResource WindowBackground}" FontFamily="Segoe UI" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="SearchBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushLight}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="45"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="839*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25" Grid.ColumnSpan="2">
            <TextBlock Text="Gestionare Studenti" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
        </StackPanel>

        <Grid Margin="0,62,0,20" Grid.ColumnSpan="2" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource SearchBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="45"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <fa5:ImageAwesome Icon="Solid_Search" Foreground="{DynamicResource TextMuted}" Width="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBox x:Name="SearchTextBox" Grid.Column="1" VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent" 
                             Foreground="{DynamicResource TextPrimary}" CaretBrush="{DynamicResource TextPrimary}"
                             Tag="Caută după Titlu, Autor sau ISBN..." Padding="5,0" FontSize="14" Margin="0,2,3,3"/>
                </Grid>
            </Border>

            <Button Grid.Column="1" Margin="15,0,0,0" Style="{StaticResource GlobalActionButtonStyle}" Foreground="White" Click="Button_Click">
                <StackPanel Orientation="Horizontal">
                    <fa5:ImageAwesome Icon="Solid_Plus" Foreground="White" Width="12" Margin="0,0,10,0"/>
                    <TextBlock Text="Adauga elev"/>
                </StackPanel>
            </Button>
        </Grid>

        <DataGrid x:Name="StudentsGrid"
          Grid.Row="2"
          Style="{StaticResource ModernDataGridStyle}"
          AutoGenerateColumns="False"
          ColumnWidth="*"
          EnableColumnVirtualization="True"
          EnableRowVirtualization="True"
          VirtualizingPanel.IsVirtualizing="True"
          VirtualizingPanel.VirtualizationMode="Recycling"
          CanUserAddRows="False"
          Margin="0,20,0,20" 
          Grid.ColumnSpan="2" SelectionChanged="StudentsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="" Width="70" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5">
                                <Ellipse Width="40" Height="40" Fill="{DynamicResource WindowBackground}"/>
                                <TextBlock Text="{Binding Initiale}" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center" 
                                           FontWeight="Bold" 
                                           Foreground="{DynamicResource TextPrimary}"
                                           FontSize="13"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Elev" Binding="{Binding NumeElev}" FontWeight="Bold"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}"/>
                <DataGridTextColumn Header="Grupa" Binding="{Binding Grupa}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{DynamicResource CardBackground}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" Padding="25" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Width="70" Height="95" Background="{DynamicResource WindowBackground}" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" Margin="0,0,20,0">
                    <Image x:Name="imagine" Source="{Binding SelectedItem.CopertaUrl, ElementName=BooksGrid}"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="DETALII ELEV" FontSize="10" FontWeight="Black" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                    <TextBlock x:Name="titlu_exemplar" Text="Selectează un elev" 
                       FontSize="22" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>

                    <WrapPanel Margin="0,8,0,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                            <fa5:ImageAwesome Icon="Solid_User" Width="12" Foreground="{DynamicResource PrimaryBlue}" Margin="0,0,5,0"/>
                            <TextBlock Text="Nume: " Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock x:Name="locatie" Text="{Binding SelectedItem.LocatieCompleta, FallbackValue='N/A'}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                            <fa5:ImageAwesome Icon="Solid_GraduationCap" Width="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,5,0"/>
                            <TextBlock Text="Clasa: " Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock x:Name="editura" Text="{Binding SelectedItem.EdituraDenumire, FallbackValue='Nespecificat'}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <fa5:ImageAwesome Icon="Solid_Book" Width="12" Foreground="{DynamicResource TextMuted}" Margin="0,0,5,0"/>
                            <TextBlock Text="Nr cărți împrumutate: " Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock x:Name="tip" Text="{Binding SelectedItem.CategorieTip, FallbackValue='0'}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource ActionButtonStyle}" Background="#222B6CB0" Foreground="#2B6CB0" Margin="0,0,10,0" Click="EditareBtn_Click">
                        <StackPanel Orientation="Horizontal">
                            <fa5:ImageAwesome Icon="Solid_UserEdit" Width="14" Margin="0,0,10,0" Foreground="#2B6CB0"/>
                            <TextBlock Text="Editare / Vizualizare"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ActionButtonStyle}" Background="#22C53030" Foreground="#C53030" Click="Button_Click_2" >
                        <fa5:ImageAwesome Icon="Solid_TrashAlt" Width="14" Foreground="#C53030"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>